# Production Environment Configuration for Railway
# DO NOT commit this file to version control
# This is a template - Railway will use environment variables

# Environment
ENVIRONMENT=production
HOST=0.0.0.0
PORT=8080

# Claude Configuration (Set in Railway dashboard)
CLAUDE_API_KEY=${CLAUDE_API_KEY}
CLAUDE_MODEL=claude-3-5-haiku-20241022
CLAUDE_DEMO_MODE=false
CLAUDE_FALLBACK_ENABLED=true

# Database Configuration (Use Railway PostgreSQL plugin)
DB_HOST=${PGHOST}
DB_PORT=${PGPORT}
DB_NAME=${PGDATABASE}
DB_USER=${PGUSER}
DB_PASSWORD=${PGPASSWORD}
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30

# Server Configuration
SERVER_URL=https://${RAILWAY_PUBLIC_DOMAIN}
SECRET_KEY=${SECRET_KEY}
ALLOWED_HOSTS=${RAILWAY_PUBLIC_DOMAIN},*.railway.app

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# Railway Configuration (Automatically set by Railway)
RAILWAY_ENVIRONMENT_NAME=${RAILWAY_ENVIRONMENT_NAME}
RAILWAY_PUBLIC_DOMAIN=${RAILWAY_PUBLIC_DOMAIN}